'use strict'

// Assert Library
var chai = require('chai');
var expect = chai.expect;
var assert = chai.assert;

// Library
var lib = require('../app/scripts/');

describe("Get word and send to google translate to get translate word ",function(){
		var userId = "111111111",
				contributeValue = 80,
				roadId = "1",
				dateStart = Date.now();
	it("send word to google translate and would get json",function(done){
		this.timeout(20000);
	});

});
